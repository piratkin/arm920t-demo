# arm920t_demo

This is demo project for build c++ code with stdc++ on the arm920t processor.

## Build

```bash
$ mkdir build
$ cd build
$ cmake .. 
$ cmake --build .
```

## Execute

```bash
$ make exec
```
